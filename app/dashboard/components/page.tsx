import Button from "@/components/Button";
import React from "react";

const Components = () => {
  return (
    <div>
      <Button label="Button" disabled={false} icon="/money-icon.svg" />
    </div>
  );
};

export default Components;
